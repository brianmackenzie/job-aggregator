# Public Fork

This repository is a clean, public-release export of a personal
jobs-aggregation project. Every YAML under `config/` ships with
generic template content that you are expected to replace before
deploying. See `docs/FORKING.md` for a full walkthrough.
