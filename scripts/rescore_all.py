"""
rescore_all.py — .

Thin CLI wrapper around the RescoreFn Lambda:
    python scripts/rescore_all.py
invokes the function asynchronously and prints the CloudWatch log group
URL so the original author can watch progress.
"""
